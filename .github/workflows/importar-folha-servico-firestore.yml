name: Importar folha de serviço Firestore

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  importar:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Baixar portal
        uses: actions/checkout@v4

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Instalar dependências
        run: npm ci

      - name: Importar folha de serviço
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_SA_PATH: ${{ runner.temp }}/firebase-sa.json
          PORTAL_JSON_TIMEOUT_MS: 180000
          FOLHA_IMPORT_JANELA: 14
        run: |
          if [ -z "${FIREBASE_SERVICE_ACCOUNT_JSON}" ]; then
            echo "Configure o secret FIREBASE_SERVICE_ACCOUNT."
            exit 1
          fi
          printf '%s' "${FIREBASE_SERVICE_ACCOUNT_JSON}" > "${FIREBASE_SA_PATH}"
          export FIREBASE_SERVICE_ACCOUNT="${FIREBASE_SA_PATH}"
          npm run import-folha-servico-firestore
